node_modules/
dist/
coverage/
.vite/
*.local
.vscode/

# Playwright visual-regression artifacts (NOT the committed baselines, which
# live in visual/__screenshots__/ and must stay tracked).
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Top-level CV data only — anchored with a leading slash so this does NOT
# also match src/data/ (resolver, schemas, tests).
/data/
!/data/base.cv.example.json

# Local Claude Code settings — contains machine-specific permissions
.claude/settings.local.json
